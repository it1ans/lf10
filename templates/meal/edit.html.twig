{% extends 'base.html.twig' %}

{% block title %}Edit Meal{% endblock %}

{% block body %}
<div class="container mt-3">
  <div class="row">
    <div class="card">
      <div class="card-body">

      <h1>Edit Meal</h1>

      {{ form(form) }}

      <a href="{{ path('app_meal_index') }}" class="btn btn-outline-secondary">back to list</a>

      {{ include('meal/_delete_form.html.twig') }}
      </div>
    </div>
  </div>
</div>
{% endblock %}
